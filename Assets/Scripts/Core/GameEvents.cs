using System;
using UnityEngine;

public static class GameEvents
{
    /* Захват объекта
    public static Action<MapObject> OnObjectCaptured;

    /* Смерть героя
    public static Action<HeroUnit> OnHeroDied;

    // Постройка завершена
    public static Action<Building> OnBuildingConstructed;

    // Начало боя
    public static Action OnBattleStarted;*/
}